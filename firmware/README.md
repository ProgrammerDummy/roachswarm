# OTA System Firmware

## Directory Structure
- `app/` - bare-metal application code with Makefile and build instructions
- `ld/` - relevant linker scripts
- `startup/` - relevant startup code and peripheral source files
- `vendor/` - cmsis vendor includes
